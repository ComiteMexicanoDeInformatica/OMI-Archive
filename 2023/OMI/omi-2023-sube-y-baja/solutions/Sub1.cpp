#include <bits/stdc++.h>

using namespace std;
void solve() {
  int n, q;
  cin >> n >> q;
  vector<int> arr(n + 1);
  for (int k = 1; k <= n; k++) {
    cin >> arr[k];
  }
  while (q--) {
    int l, r;
    cin >> l >> r;
    cout << 1 << endl;
  }
}
int main() {
  solve();
  return 0;
}
